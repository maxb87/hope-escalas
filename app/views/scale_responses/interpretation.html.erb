<% content_for :title, "Interpretação de Resultados - #{@scale_response.psychometric_scale.name} - #{@scale_response.patient.full_name}" %>

<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <!-- Header -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
          <h1 class="h3 mb-1">
            <i class="bi bi-clipboard-data me-2"></i>
            Interpretação de Resultados
          </h1>
          <p class="text-muted mb-0">
            <strong><%= @scale_response.psychometric_scale.name %></strong> - 
            <strong><%= @scale_response.patient.full_name %></strong>
          </p>
        </div>
        <div>
          <%= link_to scale_response_path(@scale_response), class: "btn btn-outline-secondary" do %>
            <i class="bi bi-arrow-left me-1"></i>
            Voltar aos Resultados
          <% end %>
        </div>
      </div>

      <!-- Informações do Paciente -->
      <div class="card mb-4">
        <div class="card-body">
          <div class="row">
            <div class="col-md-6">
              <h5 class="card-title">Informações do Paciente</h5>
              <p class="mb-1"><strong>Nome:</strong> <%= @scale_response.patient.full_name %></p>
              <p class="mb-1"><strong>Idade:</strong> <%= @scale_response.patient.age %> anos</p>
              <p class="mb-1"><strong>Data de Nascimento:</strong> <%= l(@scale_response.patient.birthday, format: :short) %></p>
            </div>
            <div class="col-md-6">
              <h5 class="card-title">Informações da Avaliação</h5>
              <p class="mb-1"><strong>Solicitado em:</strong> <%= l(@scale_response.scale_request.requested_at, format: :short) %></p>
              <p class="mb-1"><strong>Preenchido em:</strong> <%= l(@scale_response.completed_at, format: :short) %></p>
              <% if @scale_response.hetero_report? %>
                <p class="mb-1"><strong>Relator:</strong> <%= @scale_response.relator_name %> (<%= @scale_response.relator_relationship %>)</p>
              <% end %>
            </div>
          </div>
        </div>
      </div>


      <!-- Interpretação -->
      <div class="card mb-4">
        <div class="card-header">
          <h4 class="mb-0">
            <i class="bi bi-clipboard-check me-2"></i>
            Interpretação
          </h4>
        </div>
        <div class="card-body">
          <div>
            <!-- interpretacao introducao -->
            <p> <%= @scale_response.patient.full_name.titleize %> respondeu à Escala SRS-2 , instrumento que tem como objetivo
              mensurar sintomas associados ao Transtorno do Espectro Autista (TEA),
              bem como classificá-los em níveis leves, moderados ou severos. <%= @scale_response.patient.first_name.capitalize %> obteve como pontuação total neste instrumento <%=@scale_response.results.dig("metrics", "t_score")%> pontos,
              caracterizando <%= @scale_response.results.dig("metrics","levels_pluralize") %> relacionados ao TEA, de forma geral. A SRS é uma escala amplamente utilizada para avaliar a
              responsividade social e identificar sinais de comprometimento associados ao TEA.
            </p>
          </div>
          <div>
            <p> Tabela comparativa entre auto e hetero relato</p>
            <!-- Gráfico Comparativo -->
            <div class="row mb-4">
              <div class="col-12">
                <div class="card">
                  <div class="card-header">
                    <h5 class="mb-0">
                      <i class="bi bi-bar-chart me-2"></i>
                      Comparação Auto vs Hetero Relato
                    </h5>
                  </div>
                  <div class="card-body">
                    <canvas id="comparisonChart" width="400" height="200"></canvas>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div>

            <p>Interpretação por domínio de subescala</p>
            
            <% if @scale_response.problematic_domains.any? %>
              
              <% @scale_response.problematic_domains.each do |domain| %>
               
                    <h6 class="mb-0">
                      <i class="bi bi-exclamation-triangle me-2"></i>
                      <%= domain[:title] %> - 
                      <span class="badge bg-light text-dark">
                        <%= domain[:level].capitalize %>
                      </span>
                    </h6>
                  
              <% end %>
            <% end %>

            <!-- Domínios normais (exibidos de forma diferente) -->
            <% if @scale_response.normal_domains.any? %>
              <div class="mt-4">
                

                  <% @scale_response.normal_domains.each do |domain| %>
                    <div class="col-md-6 mb-2">
                      <div class="card border-success">
                        <div class="card-body py-2">
                          <div class="d-flex justify-content-between align-items-center">
                            <span class="fw-bold text-success"><%= domain[:title] %></span>
                           
                          </div>
                        </div>
                      </div>
                    </div>
                  <% end %>
                </div>

            <% end %>

            <!-- Se não há domínios problemáticos -->
            <% if @scale_response.problematic_domains.empty? %>
              <div class="alert alert-success">
                <i class="bi bi-check-circle me-2"></i>
                <strong><%= @scale_response.patient.first_name.titleize %></strong> não apresenta, de acordo com sua percepção, dificuldades significativas em nenhum dos domínios avaliados.
              </div>
            <% end %>


          </div>
          <div>
            <%= render 'scale_responses/srs2_comparison_chart' %>
          </div>
        </div>

      </div>

     

    </div>
  </div>
</div>
