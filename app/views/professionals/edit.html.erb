<% content_for :title, t('professionals.edit.title') %>

<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-header">
          <h1 class="card-title mb-0">
            <i class="bi bi-person-gear me-2"></i>
            <%= t('professionals.edit.title') %>
          </h1>
        </div>
        <div class="card-body">
          <%= render "form", professional: @professional %>
        </div>
      </div>
    </div>
  </div>
</div>
