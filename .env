# Copie este arquivo para .env para uso local (não commitar .env)

# PostgreSQL (docker-compose expõe na porta 55432)
PGHOST=localhost
PGPORT=55432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=hope_escalas_development

# Alternativamente, use DATABASE_URL (tem prioridade sobre as variáveis PG*)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/hope_escalas_development

# Redis (opcional no MVP; necessário para Sidekiq/Action Cable)
REDIS_URL=redis://localhost:6379/0

# Rails
RAILS_ENV=development
RAILS_LOG_TO_STDOUT=1
RAILS_SERVE_STATIC_FILES=1

# Mailer
MAILER_URL_HOST=localhost:3000
MAILER_FROM="Hope Escalas <no-reply@hopeneuropsiquiatria.com.br>"
